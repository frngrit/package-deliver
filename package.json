{
  "name": "package-deliver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run start --prefix frontend",
    "server": "concurrently  \"docker compose up\" \"npm install && npm run start --prefix backend\"",
    "start": "concurrently \"npm install && npm run start --prefix backend\" \"npm install && npm run start --prefix frontend\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
